<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#000000"
    android:orientation="vertical"
    android:paddingTop="5dp"
    android:windowSoftInputMode="adjustPan">

    <RelativeLayout
        android:id="@+id/content"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_above="@+id/playlistBottomPane"
        android:layout_alignWithParentIfMissing="true"
        android:orientation="vertical">

        <ListView
            android:id="@+id/song_list"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:choiceMode="singleChoice"
            android:clickable="false"
            android:fastScrollAlwaysVisible="true"
            android:fastScrollEnabled="true"
            android:focusable="false"
            android:focusableInTouchMode="false"
            android:listSelector="#660000"></ListView>

        <ImageButton
            android:id="@+id/nowPlayingToggle"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_alignParentBottom="true"
            android:background="#00FFFFFF"
            android:src="@drawable/arrowsdown" />

    </RelativeLayout>

    <fragment
        android:id="@+id/playlistBottomPane"
        android:name="com.zezo.music.tabs.playlist.PlaylistBottomPaneFragment"
        android:layout_width="match_parent"
        android:layout_height="122dp"
        android:layout_alignParentBottom="true" />

</RelativeLayout>